# Port the pair site listens on (Render/Vercel set this automatically)
PORT=3000

# Optional — only needed if Billie's AI menu features are enabled
AGNES_API_KEY=
AGNES_BASE_URL=https://apihub.agnes-ai.com/v1
